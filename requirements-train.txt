# Model training and benchmarking only. Not needed to run the service.
#
#   python3.12 -m venv .venv-train
#   ./.venv-train/bin/pip install -r requirements-train.txt
#
# Kept separate because catboost 1.2.7 requires numpy<2, which cannot coexist
# with the runtime pin of numpy==2.1.3. Installing both in one environment is
# impossible, and having catboost in requirements.txt made the whole project
# uninstallable on any Python without a prebuilt catboost wheel.
numpy<2
pandas==2.2.3
scikit-learn==1.8.0
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
shap==0.46.0
