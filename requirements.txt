# --- Sprint 1: data + models ---
# scikit-learn MUST match requirements-api.txt. Verified 2026-07-31 by reading
# the _sklearn_version stamped inside models/*.pkl: the shipped models were
# pickled by 1.8.0. This file previously pinned 1.5.2, which meant a retrain
# would emit pickles the deployment could not load safely — sklearn's own docs
# warn that cross-version loads "may lead to invalid results" silently, so this
# would have failed quietly rather than loudly.
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.8.0
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
shap==0.46.0
scipy==1.14.1
statsmodels==0.14.4
joblib==1.4.2

# --- Sprint 2+: API ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3

# --- Sprint 4: guidance layer ---
# No new dependencies. Deliberate.
#
# faiss-cpu and sentence-transformers were pinned here in anticipation of a
# vector store. They were removed after the design review: sentence-transformers
# pulls torch (~2GB) against a 512MB Render free tier, and the patient-facing
# guidance path resolves a closed nine-key enum, which is a dictionary lookup
# rather than a retrieval problem. The clinician Q&A retriever uses TF-IDF from
# scikit-learn, already pinned above for the models. See guidance/retrieval.py.

# --- Sprint 5: notifications + scheduling ---
apscheduler==3.11.0
twilio==9.3.7

# --- dev ---
pytest==8.3.4
