# Superseded — kept only so an older deploy config pointing here still works.
#
# This used to be a hand-maintained subset of requirements.txt for the API-only
# Render service. It drifted: by September 2026 it was missing pdfplumber,
# openai, httpx, voyageai and python-multipart, and still pinned anthropic
# 0.40.0 against code written for 1.x. A build from it would have succeeded and
# then failed at runtime, which is the worst of both.
#
# One list, verified from a clean virtualenv. See requirements.txt.
-r requirements.txt
